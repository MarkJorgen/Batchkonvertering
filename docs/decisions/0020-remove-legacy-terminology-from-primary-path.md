# 0020 – Remove legacy terminology from primary runtime path

The new primary runtime path for `dk.gi.app.contact.registrering.optaelling` now uses:
- local Dataverse client
- source-based ContactBLL replacement naming
- neutral workflow/client/verifier names

`legacy` terminology is retained only for:
- comparison/reference material
- historical decision notes
- legacy-reference support assets
